# Database
# For local development (SQLite):
DATABASE_URL=sqlite+aiosqlite:///./focusflow.db

# For production (PostgreSQL):
# DATABASE_URL=postgresql+asyncpg://username:password@host:port/database_name
# Example: DATABASE_URL=postgresql+asyncpg://myuser:mypassword@localhost:5432/focusflow

# API Configuration
API_V1_PREFIX=/api/v1
PROJECT_NAME=FocusFlow

# CORS Origins (comma-separated)
# For local development:
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# Alternative variable name (backward compatibility):
# BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# For production (use your actual frontend domain):
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Environment
ENVIRONMENT=development
